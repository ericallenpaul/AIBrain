# AIBrain
AIBrain is a cross-platform LLM work ledger that logs prompts, tool calls, and artifacts across assistants (Claude, OpenAI, local). It enables fast retrieval to prevent redundant work and provides a stable API for external automation. Built for power users, it supports single-user setups with possible future multi-user scalability.
